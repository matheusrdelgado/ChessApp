<Window x:Class="ChessApp.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ChessApp.WPF.ViewModel"
        mc:Ignorable="d"
        Title="CoreChess" Height="650" Width="900"
        Background="#312e2b">
    <Window.DataContext>
        <vm:GameViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>

        <Viewbox Grid.Column="0" Margin="20">

            <ItemsControl ItemsSource="{Binding BoardSquares}">

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="8" Columns="8" Width="400" Height="400"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Background="{Binding BackgroundColor}">

                            <Image Source="{Binding PieceImage}" 
                                   RenderOptions.BitmapScalingMode="HighQuality"
                                   Margin="0"/>

                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>

            </ItemsControl>
        </Viewbox>

        <StackPanel Grid.Column="1" Margin="20">
            <TextBlock Text="CoreChess" FontSize="32" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>

            <Button Content="New Game" Height="40" Margin="0,10" Background="#81b64c" Foreground="White" FontSize="16" FontWeight="Bold"/>
            <Button Content="Play vs Computer" Height="40" Margin="0,10" Background="#454545" Foreground="White" FontSize="16"/>
            <Button Content="Load Game" Height="40" Margin="0,10" Background="#454545" Foreground="White" FontSize="16"/>
        </StackPanel>
    </Grid>
</Window>
